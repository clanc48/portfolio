# Wave 1 — Site Frameworks and Page Hierarchy

1. **Blackridge Build Co.** — Django / Python
   - Home — `/`
   - Projects — `/projects/`
   - Services — `/services/`
   - Process — `/process/`
   - Contact — `/contact/`

2. **Ember & Thyme** — Next.js / TypeScript
   - Home — `/`
   - Menu — `/menu`
   - Story — `/story`
   - Events — `/events`
   - Reservations — `/reservations`

3. **Hale Mercer Legal** — Laravel / PHP
   - Home — `/`
   - Practice Areas — `/practice-areas`
   - Attorneys — `/attorneys`
   - Insights — `/insights`
   - Consultation — `/consultation`

4. **Northwell Family Health** — Flask / Python
   - Home — `/`
   - Services — `/services`
   - Providers — `/providers`
   - Patient Resources — `/patient-resources`
   - Appointments — `/appointments`

5. **Juniper Row Realty** — Next.js / TypeScript
   - Home — `/`
   - Properties — `/properties`
   - Neighborhoods — `/neighborhoods`
   - Agents — `/agents`
   - Contact — `/contact`

6. **Forge & Field Supply** — Astro / TypeScript
   - Home — `/`
   - Shop — `/shop`
   - Collections — `/collections`
   - Field Journal — `/field-journal`
   - Support — `/support`

7. **Redline Motorworks** — Next.js / TypeScript
   - Home — `/`
   - Services — `/services`
   - Builds — `/builds`
   - Performance — `/performance`
   - Book Service — `/book-service`

8. **Open Table Project** — FastAPI + Jinja / Python
   - Home — `/`
   - Programs — `/programs`
   - Impact — `/impact`
   - Get Involved — `/get-involved`
   - Donate — `/donate`

9. **OrbitStack** — SvelteKit / TypeScript
   - Home — `/`
   - Product — `/product`
   - Solutions — `/solutions`
   - Pricing — `/pricing`
   - Docs — `/docs`

10. **ClearSpring Plumbing** — Django / Python
   - Home — `/`
   - Services — `/services/`
   - Service Areas — `/service-areas/`
   - Reviews — `/reviews/`
   - Request Service — `/request-service/`

11. **The Alder House** — Nuxt 3 / Vue
   - Home — `/`
   - Rooms — `/rooms`
   - Experiences — `/experiences`
   - Journal — `/journal`
   - Book — `/book`

12. **Static Bloom Studio** — Astro / TypeScript
   - Home — `/`
   - Work — `/work`
   - Services — `/services`
   - Studio — `/studio`
   - Contact — `/contact`

