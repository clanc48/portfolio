import type {Metadata} from 'next';export const metadata:Metadata={title:'Performance'};
const rows=[['Baseline','Stock / current setup','Log temperatures, fueling, timing, boost/load, and faults.'],['Constraint review','Weakest system first','Identify heat, fuel, airflow, braking, or traction limits.'],['Modification','Matched components','Change the system intentionally rather than stacking unrelated parts.'],['Verification','Repeatable result','Re-log under comparable conditions and inspect for new limits.']];
export default function Page(){return <><section className="pageHero container"><p className="eyebrow">Measured approach</p><h1>Performance</h1><p>Power is only useful when the car can repeat it without hiding a new failure mode.</p></section><section className="container"><table className="specTable"><thead><tr><th>Stage</th><th>Question</th><th>What gets verified</th></tr></thead><tbody>{rows.map(r=><tr key={r[0]}>{r.map(c=><td key={c}>{c}</td>)}</tr>)}</tbody></table></section></>}