import type {Metadata} from 'next';
export const metadata:Metadata={title:'Agents'};
const agents=[['Mara Ellison','Buyer strategy · relocation','Known for structured search plans and neighborhood-level due diligence.'],['Theo Grant','Listings · negotiation','Focuses on positioning, pricing evidence, and clean transaction management.'],['Nina Cole','Historic homes · renovation buyers','Helps clients assess older homes without romanticizing deferred maintenance.']];
export default function Page(){return <><section className="pageHero pageHero--agents container"><p className="eyebrow">Representation</p><h1>Agents</h1><p>The profiles explain how each person works and where their experience is most useful.</p></section><section className="agentGrid container">{agents.map(([name,focus,bio])=><article className="agent" key={name}><div className="agentPortrait" aria-hidden="true"></div><p className="eyebrow">{focus}</p><h2>{name}</h2><p>{bio}</p></article>)}</section></>}
