import type {Metadata} from 'next';import ReservationForm from '../components/ReservationForm';export const metadata:Metadata={title:'Reservations'};export default function Page(){return <><section className="pageHero container"><p className="eyebrow">Join us</p><h1>Reservations</h1><p>A focused booking flow keeps the interaction simple and leaves special details for one clear notes field.</p></section><section className="reservationLayout container"><ReservationForm/><aside className="eventCard"><h2>Before you book</h2><p>Demo hours: Tuesday–Sunday, 5–10 PM.</p><p>For parties larger than six, use the events flow rather than squeezing a large-group request into a standard table booking.</p><p><strong>Demo only:</strong> no reservation data leaves the browser.</p></aside></section></>}